import {wizard} from './wizard'
import {clerik} from './clerik'

console.log('test');

console.log(wizard);

console.log(clerik);

const obj = {
    a: 'Alpha',
    b: 'Beta',
    s: 'Sigma',
    l: 'Ligma'
}

const newObj = {...obj, c: 'charlie' }